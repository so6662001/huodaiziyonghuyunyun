【任务】按现有原型实现页面（v2.7 设计已定型）

【任务卡】
- 端：[移动端 H5 / PC 端 / 双端]
- 移动端原型：docs/08-技术交付物/ui-prototype/[xx].html
- PC 端原型：docs/08-技术交付物/ui-prototype-pc/[xx].html
- 对应 PRD：docs/07-产品PRD-v2/[xx].md 第 [X.Y] 节
- 涉及 API：[METHOD] [path]
- 涉及表：[填写]
- 路由：[填写]

---

⭐ 设计已定型，你的工作是 100% 还原，不是再设计 ⭐

请按以下顺序工作：

## Step 1：定位设计资产

打开以下文件，详细阅读：
1. docs/14-AI开发提示词/13-设计资产总目录.md（找到本页对应的所有资产）
2. 对应的 PRD 章节
3. 对应的 HTML 原型
4. 对应的 API 定义
5. 对应的 DDL 表

请输出：
```
【本页设计资产清单】
- PRD：docs/07-产品PRD-v2/02-采购会话核心模块.md 第 4.1 节
- 原型（同端）：[页面 html]
- 原型（另端）：[对应另一端的 html] ← 必须对照检查一致性
- API：POST /api/v1/sessions（openapi.yaml#L120）
- 表：purchase_sessions, demand_cards
- 字段清单：sessionId / items / itemNo / quantity / ...
- 状态枚举：SessionStatus / DemandItem.status
- 关键文案：[逐条列出]
- 涉及 Design Token：--primary / --gold / --font-size-15 ...
```

## Step 2：检查多规格红线（如涉及）

打开 docs/14-AI开发提示词/14-多规格红线.md

请回答：
```
□ 本页是否涉及多规格场景？是 / 否
□ 如是，对照红线列表逐项检查
  □ items 数组（非单值）
  □ 行号 itemNo 稳定
  □ 整单+行级分层
  □ 行级 6 种状态支持
  □ 行内编辑（非弹窗）
  □ ...
```

## Step 3：检查端一致性（如双端）

打开 docs/14-AI开发提示词/15-页面对应与不走样规范.md

请列出：
```
【对端对照】
对应另一端：[文件路径]

字段对照表（必须 100% 一致）：
| 字段 | 本端 | 另端 |
|---|---|---|
| sessionId | ✓ | ✓ |
| ...

状态对照表：
| 枚举 | 值 | 显示文案 |
|---|---|---|
| status | dispatching | 派单中 |
| ...

文案对照表：
| 按钮/标签 | 本端 | 另端 |
|---|---|---|

颜色/图标对照表：
| 业务含义 | Token | 两端 |
|---|---|---|
```

## Step 4：实现

按以下顺序：

### 4.1 复用既有组件
检查 styles.css（移动端）/ styles-pc.css（PC）中已有的：
- card / btn / form-row / phone-frame / tab-bar
- spec-row / list-card / matrix / kpi-card
- 列出您将复用的组件

### 4.2 实现新组件（如必须）
- 严格用 Token
- 严格按 5px 网格

### 4.3 实现页面 HTML/Vue
- 严格按原型还原
- 严格用 Token
- 不写一次性内联样式

### 4.4 接入 API
- 路径必须对应 openapi.yaml
- 字段必须对应 DDL

## Step 5：完整性自检

按以下 5 维度自检并输出：

```
## 完整性自检

【1. 字段一致性】
- 所有字段名与 DDL 一致 ✓
- 所有字段名与 OpenAPI 一致 ✓
- 所有字段名两端一致 ✓

【2. 状态一致性】
- 所有状态值在标准枚举内 ✓
- 显示文案两端一致 ✓

【3. 设计一致性】
- 所有颜色用 Token ✓
- 所有字号用 Token ✓
- 所有间距 4 倍数 ✓
- 按钮高度 >= 44pt（移动端 CTA）✓
- 与原型像素级对照差异 < 5%

【4. 多规格支持】（如涉及）
- items 数组 ✓
- 按行操作 ✓
- 行级状态 ✓
- 6 种状态完整 ✓

【5. 50 条铁律】
- A 类字段 ✓ A01-A10 全部
- B 类 API ✓ B01-B10 全部
- D 类 UI ✓ D01-D10 全部
- E 类多规格 ✓ E01-E10（如涉及）
```

## Step 6：输出报告

```
## 开发报告

### 文件清单
新增：xxx.vue / xxx.html
修改：xxx.scss / xxx-store.js
路由：pages.json 已更新

### 复用情况
- 复用基础组件 N 个：[列出]
- 复用业务组件 M 个：[列出]
- 新增组件 K 个：[列出 + 理由]

### 与原型对照
- 整体布局：100% 一致
- 颜色/字号：100% 用 Token
- 关键差异：[列出，如有合理调整]

### 跨端一致性
- 字段：✓ 全部一致
- 状态：✓ 全部一致
- 文案：✓ 全部一致

### 测试
- 单元测试覆盖率：xx%
- 关键场景：[列出已测]

### 已知问题 / Trade-off
- [列出]
```

【红线】
🚫 不准创造原型没有的元素
🚫 不准遗漏原型有的元素
🚫 不准用 Token 之外的颜色/字号
🚫 不准与另一端字段/状态不一致
🚫 不准退化多规格场景

【请开始 Step 1】
