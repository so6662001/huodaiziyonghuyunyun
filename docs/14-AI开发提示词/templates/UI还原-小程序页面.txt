【任务】把 HTML 原型还原为小程序页面

【输入】
- HTML 原型：docs/08-技术交付物/ui-prototype/[xx].html
- 设计规范：docs/13-Figma设计规范/01-设计系统总览.md
- Design Tokens：docs/13-Figma设计规范/design-tokens/tokens.json
- 输出路径：pages/[buyer|seller|common]/[xxx]/index.vue
- 端：买家小程序

---

请按以下顺序工作：

## Step 1：阅读 HTML 原型

请打开 HTML，并输出：

### 1.1 视觉元素清单

| 元素 | 描述 | 大概位置 |
|---|---|---|
| 顶部 nav | 标题 + 返回按钮 | 顶部 |
| 主标题 | "发布采购需求" | nav 下方 |
| ... | ... | ... |

### 1.2 颜色提取

| 用途 | 原型颜色 | 应映射 token | 理由 |
|---|---|---|---|
| 标题文字 | #1a2233 | $color-text-primary | 主文字色 |
| 主按钮 | #2563eb | $color-primary-600 | 品牌主色 |
| 加急标签 | #f59e0b | $color-warning-500 | 警告色 |
| ... | ... | ... | ... |

### 1.3 字号提取

| 用途 | 原型字号 | 应映射 token |
|---|---|---|
| 主标题 | 22px | $font-size-22 |
| 正文 | 15px | $font-size-15 |
| ... | ... | ... |

### 1.4 间距提取

| 用途 | 原型间距 | 应映射 token |
|---|---|---|
| 页面 padding | 16px | $space-5 |
| 卡片间距 | 12px | $space-3 |
| ... | ... | ... |

### 1.5 组件清单

复用既有组件：
- Button（默认 / Primary / Warning）
- Input（默认）
- Card（默认）
- ...

需要新建组件：
- QuantityInput（数量 + 单位 + 步进器）
- ...

### 1.6 交互行为

1. 点击 [元素] → [行为]
2. 输入 [字段] → [行为]
3. ...

### 1.7 状态清单

- 默认：表单空
- 输入中：实时校验
- 加载：禁用按钮 + 转圈
- 错误：toast + 字段高亮
- 成功：toast + 跳转

## Step 2：实现

### 2.1 写 pages.json 注册路由

### 2.2 实现页面 .vue

```vue
<template>
  <!-- 严格按 HTML 原型还原 -->
</template>

<script setup>
// 状态管理
// API 调用
// 表单校验
</script>

<style lang="scss" scoped>
@import "@/styles/tokens.scss";

// 所有样式必须用 token，不准硬编码
</style>
```

### 2.3 拆分子组件（如需）

### 2.4 写 API 调用（api/[module].js）

### 2.5 写 Store（如需）

## Step 3：自查

请按以下 Checklist 自查并输出结果：

```
□ 所有颜色都用了 token（grep -r "#[0-9a-fA-F]" 应该为 0）
□ 所有字号都用了 token（grep -r "font-size: [0-9]" 应该为 0）
□ 所有间距都是 4 的倍数
□ 所有按钮高度 ≥ 44pt（关键 CTA）/ 28pt（次要）
□ 圆角符合规范：按钮 8 / 卡片 12 / 标签 4
□ 包含默认 / 加载 / 空 / 错 / 成功 5 个状态
□ 表单防双击
□ 表单有校验
□ 错误提示是人话
□ 关键操作二次确认
□ 长文本省略
□ 数字千分位
□ 价格保留 2 位小数
□ 时间相对显示
□ 图片加载失败 fallback
□ 移动端 375 / 414 双适配
□ scoped 样式 + BEM 命名
□ Props 有完整类型
□ 无 console.log 残留
```

## Step 4：与原型对比

请描述：
1. 我实现的页面布局是什么样的（用文字描述）
2. 与原型对比，有哪些差异
3. 差异的合理性（如有必要修改）
4. 是否需要二次还原

## Step 5：输出报告

```
## UI 还原报告

### 文件清单
新增：
- pages/buyer/demand/create/index.vue（135 行）
- components/business/quantity-input/index.vue（72 行）

### Token 引用清单
颜色（6 个）：
- $color-primary-600（CTA）
- $color-text-primary（标题）
- $color-text-secondary（正文）
- $color-bg-page（背景）
- $color-bg-card（卡片）
- $color-warning-500（加急）

字号（4 个）：
- $font-size-22（主标题）
- $font-size-16（小标题）
- $font-size-15（正文）
- $font-size-12（辅助）

间距（5 个）：
- $space-5（页面 padding）
- $space-4（字段间距）
- $space-3（行间距）
- $space-2（小间距）
- $space-1（极小）

### 与原型对比
✓ 整体布局：完全一致
✓ 颜色：完全一致（用 token）
✓ 字号：完全一致
⚠ 加急标签位置：原型在右上，实现在右中（建议调整）

### 状态完整性
✓ 默认 / 加载 / 空 / 错 / 成功 全部实现

### 移动端适配
✓ 375px 测试通过
✓ 414px 测试通过
```

【红线】

🚫 不准硬编码颜色 / 字号 / 间距
🚫 不准字号 < 11px
🚫 不准按钮 < 28pt
🚫 不准 wx.request 直接用
🚫 不准 v-html
🚫 不准忽略状态
🚫 不准忽略边界

【请开始 Step 1】
